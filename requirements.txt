# Core Dependencies
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Deep Learning Frameworks
openvino>=2024.0.0
#openvino-dev>=2024.0.0  # For model conversion tools
# tensorrt>=8.6.0  # Optional: For NVIDIA GPU acceleration (requires separate installation)
# pycuda>=2022.2  # Optional: Required for TensorRT

# GUI Framework
PySide6>=6.5.0
qasync>=0.24.0  # Async support for Qt
pyqtgraph>=0.13.0  # Real-time plotting

# Object Tracking
sort-tracker-py>=1.0.0  # SORT tracker
# For ByteTrack, install from source:
# pip install git+https://github.com/ifzhang/ByteTrack.git

# Data Processing
pandas>=2.0.0
scipy>=1.10.0

# Database
aiosqlite>=0.19.0

# API and Networking
aiohttp>=3.8.0
backoff>=2.2.0  # Retry logic
requests>=2.31.0  # Fallback for sync requests

# Configuration and Validation
pydantic>=2.0.0
python-dotenv>=1.0.0

# Logging
structlog>=23.1.0

# Export and Reporting
xlsxwriter>=3.1.0
openpyxl>=3.1.0
reportlab>=4.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Utilities
Pillow>=10.0.0
psutil>=5.9.0
click>=8.1.0  # CLI support

# Development Tools (optional)
pytest>=7.3.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-qt>=4.2.0
black>=23.3.0
isort>=5.12.0
mypy>=1.3.0
pre-commit>=3.3.0

# Documentation (optional)
sphinx>=7.0.0
sphinx-rtd-theme>=1.2.0
sphinx-autodoc-typehints>=1.23.0

# Performance Profiling (optional)
line-profiler>=4.0.0
memory-profiler>=0.61.0

# Platform-specific dependencies
# Windows
pywin32>=306; sys_platform == 'win32'

# Linux
python-xlib>=0.33; sys_platform == 'linux'

# Optional Machine Learning Models
# ultralytics>=8.0.0  # YOLOv8 (if needed)
# mmcv-full>=1.7.0  # OpenMMLab (if needed)
# mmdet>=3.0.0  # MMDetection (if needed)

# Note: Some dependencies may require specific installation steps:
#
# For OpenVINO:
# pip install openvino-dev[tensorflow2,onnx,pytorch]
#
# For TensorRT (